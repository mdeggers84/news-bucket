<div id="navbar">
  <button id="scrape-btn" class="btn btn-submit">Scrape</button>
</div>
<div id="news">
  {{#each doc}}
  <ul>
    <li>{{this.title}}</li>
    <li><a href="{{this.link}}">{{this.link}}</a></li>
  </ul>
  <button data-id="{{this._id}}" class="save-btn">Save</button> {{/each}}
</div>
<div id="comments"></div>

<script src="assets/js/index.js"></script>